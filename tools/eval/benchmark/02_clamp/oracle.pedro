expect:
    clamp(5, 0, 10) == 5
    clamp(1, 3, 10) == 3
    clamp(15, 0, 10) == 10
    clamp(3, 3, 10) == 3
    clamp(10, 0, 10) == 10
    for all x from 0 to 40: clamp(x, 10, 20) is at least 10
    for all x from 0 to 40: clamp(x, 10, 20) is at most 20
