# Sum of a list

Write a task that returns the sum of a list of whole numbers. The sum of an empty
list is `0`.

## Required signature

    task sum_list(xs: list of whole) returns whole

## Details

- `sum_list([1, 2, 3, 4])` is `10`.
- `sum_list([])` is `0`.
- `sum_list([42])` is `42`.
